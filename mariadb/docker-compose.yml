version: "3.7"

services:
  mariadb:
    image: mariadb:latest
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: 123456

  adminer:
    image: adminer:latest
    restart: always
    ports:
      - 7000:8080
